# maze-generation-with-sfml



### Build Instructions 

In root directory, type the following commands:

```cmake
cmake -B build 
cmake --build build
```

To run the executable:

```make 
./build/bin/amaze 
```
